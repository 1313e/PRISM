n_sam_init		: 500			# Number of initial model evaluation samples
proj_res		: 15			# Number of projected grid points per model parameter
proj_depth		: 150			# Number of emulator evaluation samples per projected grid point
n_eval_samples		: 800			# Base number for growth in number of model evaluation samples
sigma			: 0.8			# Gaussian sigma (only required if method == 'gaussian')
l_corr			: 0.3			# Gaussian correlation length
impl_cut		: [0.0, 4.0, 3.8, 3.5]	# List of implausibility cut-off values
criterion		: 'multi'		# Criterion for constructing LHDs
method			: 'full'		# Method used for constructing the emulator system
use_regr_cov		: False			# Use regression covariance
poly_order		: 3			# Polynomial order for regression (only required if method != 'gaussian')
do_active_anal		: True			# Perform active parameter analysis
freeze_active_par	: True			# Active parameters always stay active
pot_active_par		: None			# List of potentially active parameters
use_mock		: False			# Use mock data
